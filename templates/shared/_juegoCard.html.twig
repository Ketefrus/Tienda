           <div class="card text-white bg-dark shadow m-2" style="width: 18rem;">
                <img height="260" width="60" class="card-img-top" src="{% if juego.imagen is empty %}/imgs/noimg.png{% else %}{{ asset(juego.imagen) }}{% endif %}">
                <div class="card-body">
                    <h5 class="card-title">
                    {{ juego.nombre }}
                    </h5>
                    <p class="card-text"> {{ juego.descripcion }} </p>
                    <div class="row">
                        <div class="col text-muted">{{ juego.precio }}€ </div>
                    </div>
                </div>
                <div class="card-footer bg-transparent gap-2 d-md-flex justify-content-md-end">
                    <a class="btn btn-sm btn-outline-info" href="{{ path('juego_show', {'id': juego.id}) }}">show</a>
                    <a class="btn btn-sm btn-primary" href="{{ path('juego_edit', {'id': juego.id}) }}">edit</a>
                </div>
           </div>