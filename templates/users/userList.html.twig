{% extends 'base.html.twig' %}
{% block body %}
  <div class="container">
    <table class="table table-dark table-striped">
      <thread>
        <tr>
          <th scope="col">
            {{ 'Nombre'|trans }}
          </th>
          <th scope="col">
            {{ 'Contraseña'|trans }}
          </th>
          <th scope="col">
            email
          </th>
        </tr>
      </thread>
      <tbody>
        {% for usuario in usuarios %}
          <tr>
            <td>
              {{ usuario.nombre }}
            </td>
            <td>
              {{ usuario.password }}
            </td>
            <td>
              {{ usuario.email }}
            </td>
          </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
{% endblock %}
