{% extends 'base.html.twig' %}

{% block title %}{{'Editar'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Editar'|trans}}</h1>

    {{ include('juego/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('juego_index') }}">{{'Volver a inicio'|trans}}</a>

    {{ include('juego/_delete_form.html.twig') }}
{% endblock %}
