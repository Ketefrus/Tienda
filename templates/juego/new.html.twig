{% extends 'base.html.twig' %}

{% block title %}{{'Nuevo'|trans}}{% endblock %}

{% block body %}
<div class="container">
<div class="card bg-light mt-4">
 <div class="card-header">
    <h1>{{'Añade un juego'|trans}}</h1>
</div>
    {{ include('juego/_form.html.twig') }}

    <a href="{{ path('juego_index') }}">{{'Volver'|trans}}</a>
</div>
</div>
{% endblock %}
