{% extends 'base.html.twig' %}

{% block title %}
  Juego
{% endblock %}

{% block body %}
  <h1>
    {{ 'Juego'|trans }}
  </h1>

  <table class="table table-striped">
    <tbody>
      <tr>
        <th scope="col">
          {{ 'Nombre'|trans }}
        </th>
        <td>
          {{ juego.nombre }}
        </td>
      </tr>
      <tr>
        <th scope="col">
          {{ 'Precio'|trans }}
        </th>
        <td>
          {{ juego.precio }}
        </td>
      </tr>
      <tr>
        <th scope="col">
          {{ 'Descripción'|trans }}
        </th>
        <td>
          {{ juego.descripcion }}
        </td>
      </tr>
      <tr>
        <th scope="col">
          {{ 'Propietario'|trans }}
        </th>
        <td>
          {{ juego.propietario.nombre }}
        </td>
      </tr>
    </tbody>
  </table>

  <a href="{{ path('juego_index') }}">{{ 'Volver'|trans }}</a>

  <a href="{{
    path(
      'juego_edit',
      {
        id: juego.id
      }
    )
    }}">
    {{ 'Editar'|trans }}
  </a>

  {{ include('juego/_delete_form.html.twig') }}
{% endblock %}
